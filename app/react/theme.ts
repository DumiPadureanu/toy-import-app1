import { ThemeProvider } from '@ui5/webcomponents-react/dist/ThemeProvider';

export const theme = {
  // UI5 theme configuration
  // Fiori themes: 'sap_horizon', 'sap_fiori_3', etc.
  defaultTheme: 'sap_horizon'
};

export default theme;
